# optional: bool,
# optional_with: Option<Vec<String>>,
# version: Option<VersionReq>,
# on_level: DepOnLevel,
# collection: PackageType,

[dependency.bash]
optional = false
on_level = "Commander" # Node | Group | Project | Commander / Client
collection = "Executor"